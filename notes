Xrepo acme-cms
Xadd db.js file
create the model which will get the code below to work as expected
(note the hierarchy method is tricky and you don't need it for the routes)<---WORKING ON THIS
Xadd server.js file
Xadd route for /api/pages which will return all pages
add route for /api/pages/:id/children which will return the child pages for a page with passed in id
add route for /api/pages/:id/siblings which will return the siblings for a given passed in id



Prof notes- no react

just express and sequelize

myNotes

make a model
make methods???
X-findHomePage() //class method
X-findChildren()  //instance method
X-findOne()   I think this is a built-in funct
-hierarchy() instance<---WORKING ON THIS
